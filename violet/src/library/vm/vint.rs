//! Virtual (external)Interrupt

/*
const NUM_OF_INTERRUPTS: usize = 32;

pub struct VirtualInterrupt {
    handler: [Option<fn(regs: *mut usize)>; NUM_OF_INTERRUPTS],
}

impl VirtualInterrupt {
    pub fn register();
    pub fn call();
    pub fn assert();
}
*/
