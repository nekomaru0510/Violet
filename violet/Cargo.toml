[package]
name = "violet"
version = "0.5.0"
authors = ["nekomaru0510 <yama05rymy@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_device, values("qemu_virt", "hifive_premier_p550"))'] }

[dependencies]
spin = "0.9.8"
linked_list_allocator = "0.10.5"
